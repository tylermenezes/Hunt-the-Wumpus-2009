using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HuntTheWumpus.WorldAndStuff.Weapons
{
    class Wympus6Weapon : WeaponTypes.ProjectileWeapon
    {
        public Wympus6Weapon(DynamicEntity host)
            : base(host,
            25, // set damage here (10-75)
            0,  // set value here
            300, // set attack radius here 
            50)  // set recharge time here ( milliseconds should be > 50)
        {
            // That's all, do nothing here
        }

        protected override void loadProjectile()
        {
            throw new NotImplementedException();
        }
    }
}
