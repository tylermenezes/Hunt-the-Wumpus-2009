using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HuntTheWumpus.WorldAndStuff.Weapons
{
    class PlayerWeapon3 : WeaponTypes.ProjectileWeapon
    {
        public PlayerWeapon3 (DynamicEntity host)
            : base(host,
            10, // set damage here (10-75)
            100, // set value here
            100, // set attack radius here 
            50)  // set recharge time here ( milliseconds should be > 50)
        {
            // That's all, do nothing here
        }

        protected override void loadProjectile()
        {
            throw new NotImplementedException();
        }
    }
}
